# Optional: MongoDB API (see server/.env.example). When set, the app uses the Express + Mongo backend.
# VITE_API_URL=http://localhost:4000

# Optional: Supabase (Google OAuth + legacy Postgres path). Omit VITE_API_URL if you want Supabase-only data.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
